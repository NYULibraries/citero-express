# root = require('../lib/citero')
# Citero = root.Citero
# Formats = root.Formats
# chai = require 'chai'
# expect = chai.expect
#
# CSF = require('./helpers/csf').CSF
# console.log Citero.fromFormats()
# for csf in CSF
#   describe "Citero mapping", ->
#     it "should convert CSF data to OpenURL", ->
#       expect(Citero.map(csf.data).from(csf.format).to("HANNAN")).to.equal(csf.toOpenUrl)
#     it "should convert CSF data to RIS", ->
#       expect(Citero.map(csf.data).from(csf.format).to("RIS")).to.equal(csf.toRis)
#     it "should convert CSF data to Refworks Tagged", ->
#       expect(Citero.map(csf.data).from(csf.format).to("REFWORKS_TAGGED")).to.equal(csf.toRefworksTagged)
#     it "should convert CSF data to bibtex", ->
#       expect(Citero.map(csf.data).from(csf.format).to("BIBTEX")).to.equal(csf.toBibtex)
#     it "should convert CSF data to easybib", ->
#       expect(Citero.map(csf.data).from(csf.format).to("EASYBIB")).to.equal(csf.toEasyBib)
#     it "should convert CSF data to csl", ->
#       expect(Citero.map(csf.data).from(csf.format).to("CSL")).to.equal(csf.toCsl)
#     it "should convert CSF data to csf", ->
#       expect(Citero.map(csf.data).from(csf.format).to("CSF")).to.equal(csf.toCsf)
